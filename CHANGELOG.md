# 更新日志 · WordIsle「我岛」

> 本页记录每个版本的变化。应用内「📖 更新日志」文档随应用版本附带（无需联网），这里的公开仓库版本供开发者查阅。

## v1.1.0 · 体验升级与安全加固

本次更新带来大量新功能与体验改进，并修复了 v1.0.0 的若干问题。

### 新功能

- **联网检查更新**：启动时静默查询 GitHub Releases，发现新版本在顶栏提示横幅，可一键跳转下载页
- **更新日志常驻文档**：「📖 更新日志」文档会自动拉取本文件最新内容，离线时保留兜底文案
- **数据目录可配置**：在「偏好设置」中自选数据存储位置，支持一键迁移已有数据
- **应用菜单与快捷键**：新增标准菜单（文件 / 编辑 / 视图 / 窗口），支持 `Cmd+,` 偏好设置、`Cmd+N` 新建、`Cmd+O` 导入、`Cmd+E` 导出、`Cmd+D` 夜间模式、`Cmd+F` 专注模式等快捷键
- **双击文件导入**：在访达中双击 `.myisland` 文件可直接导入到「我岛」
- **DMG 安装引导**：从 DMG 直接运行时会提示自动安装到 `/Applications`，自动清除隔离属性并推出 DMG

### 体验改进

- **字体本地化**：思源宋体（Noto Serif SC）与霞鹜文楷（LXGW WenKai）字体打包到应用内，不再依赖网络 CDN，离线可用且加载更快
- **数据版本控制**：引入 `__version` 字段，为未来数据结构升级预留迁移机制
- **设置持久化**：字体、字号、夜间模式等设置从 localStorage 迁移到数据文件，换机不丢设置
- **DMG 背景图优化**：修复 v1.0.0 背景图消失、图标过小不居中的问题

### 安全与稳定性

- **原子写入**：数据文件采用「临时文件 + rename」原子写入，避免崩溃 / 断电导致数据损坏
- **内容安全策略（CSP）**：限制资源加载来源，仅放行 GitHub API 用于检查更新
- **安全加固**：关闭 Node.js 集成、开启上下文隔离与同源策略、外链跳转仅放行 `github.com`
- **单实例锁**：防止多开导致配置冲突
- **关闭时同步落盘**：窗口关闭前同步写入数据，确保内容不丢

### 其他

- 许可证统一为闭源（安装包可免费下载使用，详见 LICENSE）
- 应用未签名，macOS 首次安装仍需执行 `xattr -cr` 清除隔离属性（详见 README 安装步骤）

> 📌 「🏝️ 岛屿」与「🏆 成就」游戏化模块仍为占位状态，持续开发中。

---

## v1.0.0 · 首发功能

欢迎使用「我岛」——一款专注写作记录的桌面应用，用游戏化激励让你坚持每天写字。数据完全本地，离线可用，零上传。

### 文档与标签

- 文档管理：文档 / 文件夹多层嵌套 + 拖拽排序 + 全文搜索
- 标签系统：5 类预置标签（篇幅 / 体裁 / 题材 / 原创性 / 状态）+ 自定义标签 + 多选 AND 筛选（类 Steam 游戏库）
- 标签管理：体裁 / 题材分类可右键重命名 / 删除预置标签

### 字数与看板

- 实时字数统计（含标点，英文按单词）+ 字数日历热力图（5 档颜色）
- 6 维度看板：日 / 周 / 月 / 年 / 总计 / 自定义区间 + 趋势图表（柱状 / 折线 / 叠加）
- 码字计划 + 当日备忘与心情记录

### 排版与编辑

- 段落对齐（左 / 中 / 右）+ 粗体 / 斜体 + 撤回 / 重做
- 一键排版（保留空行）+ 首行缩进
- 14–22px 字号 / 3 款中文字体 / 字符间距调节 / 侧栏可拖拽调宽

### 体验与数据

- 专注模式 + 夜间模式
- 5 秒自动保存 + Ctrl/Cmd+S 手动保存 + 顶部保存按钮
- 单实例锁防止多开冲突
- 数据完全本地存储，零上传，零遥测
- 导入 / 导出 .myisland 备份文件，换机无忧

> 📌 「🏝️ 岛屿」与「🏆 成就」为持续开发中的游戏化模块，目前为占位状态。

---

愿你在文字的小岛上，开启属于自己的新旅程。
